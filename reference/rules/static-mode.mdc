---
description: Static pipeline rules for Deep Glitch Files (no I2V — shots + Ken Burns only)
globs: ["**/dgf-static-pipeline/**", "**/templates/episode/**", "**/canon/**"]
alwaysApply: false
---

# Deep Glitch Files — STATIC MODE (рабочий режим)

Оверлей поверх `pipeline.mdc`. Всё не упомянутое здесь — из `pipeline.mdc` без изменений
(TTS, cost gate, skip-if-exists, canon-first cast, banned Flux, two-pass loudnorm, act card audio).

---

## ОТКЛЮЧЕНО

- ❌ **Любой I2V** (Seedance 2.0 Standard/Fast, Kling) — включая act cards T1-T3
- ❌ **End-frames** (gpt2edit пары start→end для переходов)
- ❌ **Motion-промпты** (3-layer `[Subject] | [Camera] | [Lighting]`, 6 rules)
- ❌ **SHOT TAXONOMY Kling vs Seedance** — не применяется
- ❌ **`gen-i2v-*.ts`** — не запускать на новых эпизодах

## ЗАМЕНЕНО

| Motion (EP002) | Static (рабочий) |
|---|---|
| `assemble.ts` (I2V clips) | `assemble-final.ts` (Ken Burns на PNG) |
| Static-test = черновик перед I2V | Static-test = **почти финал** — смотреть придирчиво |
| Character-proportional beats | `calc-timings.ts` → 5s auto-grid → `vo/timings.json` + `vo/shot-order.json` |
| Seedance на все 45 шотов (~$62) | FFmpeg zoompan ($0) |

## DGF STATIC ИНВАРИАНТЫ

1. **Голос**: DukeClay `Mn8ouzOen8s5FX5UTC0M` — не DukeFull, не другие
2. **Preamble**: `canon/VISUAL_CANON.md` §3 в КАЖДОМ gen-frames промпте
3. **Glitch-блок preamble**: только ACT2 reveal (1-3 шота), не весь эпизод
4. **Каст**: canon-first — `canon/cast/*-doodle.png` как ref. Clay refs = LEGACY (EP001-02 only)
5. **Текст в кадре**: AI не рисует — FFmpeg overlay (file-stamp, redaction)
6. **Act cards**: статика + Ken Burns + `canon/bumpers/glitch-sting.m4a` @45%, 3.5s. Тишина запрещена
7. **Музыка**: 3-zone (Artifact → Muted System → Campfire) @15%, fallback single + acrossfade
8. **Шоты 5s cadence** (LOCKED EP003+): `calc-timings.ts` режет VO на ≤5s ячейки; промпты в `shots.ts`
9. **assemble-final/static** читают `vo/shot-order.json` — не хардкодить SEGMENTS
10. **Bumpers**: `canon/wrap-with-bumpers.ts` после EP-final.mp4 — **`--force` после rebuild**; не копировать wrapped вручную
11. **Upload**: `canon/upload-youtube.ts` — auto re-wrap если stale + cadence gate
12. **Wonder-first**: SCRIPT_ENGINE critic-gate до gen-vo

## СТОИМОСТЬ

~67 шотов @ 5s: **~$5–7** (VO + frames + thumbs). Cost gate >$7 — проверить лишние regen.

---

*2026-08-14. Спека: `pipeline/STATIC_PIPELINE_REFERENCE.md`. Шаблоны: `pipeline/templates/episode/`.*
